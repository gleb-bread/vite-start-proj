import { type ThemeDefinition } from 'vuetify'
import { Colors } from '@/plugins/vuetify/colors'
import { Varibles } from '@/plugins/vuetify/varibles'

export const Default: ThemeDefinition = {
  dark: false,
  colors: Colors.Default,
  variables: Varibles.Default
}
